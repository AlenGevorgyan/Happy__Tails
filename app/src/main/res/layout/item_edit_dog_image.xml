<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="8dp"> <ImageView
    android:id="@+id/imageViewEdit"
    android:layout_width="120dp" android:layout_height="120dp" android:scaleType="centerCrop"
    android:contentDescription="Gallery Image"
    android:background="@color/light_gray"/> <ImageButton
    android:id="@+id/button_delete_image"
    android:layout_width="36dp"
    android:layout_height="36dp"
    android:layout_alignParentTop="true"
    android:layout_alignParentEnd="true"
    android:layout_marginTop="4dp"
    android:layout_marginEnd="4dp"
    android:background="@drawable/circle_background_red" android:elevation="4dp"
    android:padding="8dp"
    android:src="@drawable/ic_cancel" app:tint="@color/white"
    android:contentDescription="Delete Image"/>

</RelativeLayout>
